---
// Pricing section component
const plans = [
  {
    name: "Developer",
    description: "Perfect for individual developers",
    price: 49,
    frequency: "monthly",
    requests: "10,000",
    features: [
      "10,000 API requests/month",
      "Basic natural language processing",
      "Single database connection",
      "Standard response time",
      "Community support"
    ],
    buttonText: "Start Free Trial",
    popular: false
  },
  {
    name: "Business",
    description: "Ideal for growing companies",
    price: 199,
    frequency: "monthly",
    requests: "100,000",
    features: [
      "100,000 API requests/month",
      "Advanced context understanding",
      "Multiple database connections",
      "Query optimization",
      "Priority support",
      "Custom integrations"
    ],
    buttonText: "Start Free Trial",
    popular: true
  },
  {
    name: "Enterprise",
    description: "Custom solutions for large organizations",
    price: null,
    frequency: "custom",
    requests: "Unlimited",
    features: [
      "Unlimited API requests",
      "Full context awareness",
      "Unlimited database connections",
      "Advanced security features",
      "Dedicated support team",
      "Custom deployment options",
      "SLA guarantees"
    ],
    buttonText: "Contact Sales",
    popular: false
  }
];

---

<section id="pricing" class="relative isolate bg-gradient-to-b from-white/80 to-white px-6 py-20 lg:px-8 -mt-10">
  <div class="absolute inset-x-0 -top-3 -z-10 transform-gpu overflow-hidden px-36 blur-3xl" aria-hidden="true">
    <div class="mx-auto aspect-[1155/678] w-[72.1875rem] bg-gradient-to-tr from-primary to-tech opacity-10" style="clip-path: polygon(74.1% 44.1%, 100% 61.6%, 97.5% 26.9%, 85.5% 0.1%, 80.7% 2%, 72.5% 32.5%, 60.2% 62.4%, 52.4% 68.1%, 47.5% 58.3%, 45.2% 34.5%, 27.5% 76.7%, 0.1% 64.9%, 17.9% 100%, 27.6% 76.8%, 76.1% 97.7%, 74.1% 44.1%)"></div>
  </div>

  <div class="text-center mb-16">
    <span class="inline-block px-4 py-2 rounded-full bg-primary/10 text-primary font-medium text-sm mb-4 font-secondary">
      Flexible Pricing
    </span>
    <h2 class="text-3xl font-bold mb-4 text-text font-primary">Choose Your Plan</h2>
    <p class="text-gray-600 max-w-2xl mx-auto font-secondary">
      Start with our free trial and scale as your needs grow. All plans include our core natural language processing features.
    </p>
  </div>

  <div class="mx-auto mt-16 grid max-w-7xl grid-cols-1 items-center gap-8 lg:grid-cols-3">
    {plans.map((plan) => (
      <div class={`rounded-3xl relative transition-all duration-300 ${
        plan.popular 
          ? 'p-8 bg-white shadow-2xl scale-105 z-10 border-2 border-tech/20' 
          : 'p-6 bg-white/80 backdrop-blur-sm shadow-lg hover:shadow-xl border border-tech/10'
      }`}>
        {plan.popular && (
          <div class="absolute -top-5 left-0 right-0 mx-auto w-fit">
            <span class="bg-gradient-to-r from-primary to-tech text-white text-sm font-semibold px-4 py-1 rounded-full font-secondary">
              Most Popular
            </span>
          </div>
        )}

        <div class="text-center mb-8">
          <h3 class={`font-primary font-bold mb-2 ${plan.popular ? 'text-2xl text-text' : 'text-xl text-text'}`}>{plan.name}</h3>
          <p class="text-gray-600 font-secondary mb-6">{plan.description}</p>
          
          {plan.price ? (
            <div class="flex items-center justify-center gap-2">
              <span class={`font-bold ${plan.popular ? 'text-4xl text-text' : 'text-3xl text-text'}`}>${plan.price}</span>
              <span class="text-gray-500 font-secondary">/{plan.frequency}</span>
            </div>
          ) : (
            <div class="font-bold text-3xl text-text">Custom Pricing</div>
          )}
        </div>

        <div class="bg-tech/5 rounded-xl p-4 mb-8">
          <div class="text-center">
            <span class="text-tech font-bold text-2xl font-primary">{plan.requests}</span>
            <span class="text-gray-600 font-secondary block text-sm">API Requests</span>
          </div>
        </div>

        <ul class="space-y-4 mb-8">
          {plan.features.map((feature) => (
            <li class="flex items-center text-gray-600 font-secondary">
              <svg class="w-5 h-5 text-primary mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
              {feature}
            </li>
          ))}
        </ul>

        <button class={`w-full py-3 px-6 rounded-xl transition-all duration-300 font-secondary ${
          plan.popular 
            ? 'bg-gradient-to-r from-primary to-tech text-white hover:shadow-lg hover:shadow-tech/20' 
            : plan.price === null
              ? 'bg-text text-white hover:bg-text/90'
              : 'bg-tech/10 text-tech hover:bg-tech/20'
        }`}>
          {plan.buttonText}
        </button>
      </div>
    ))}
  </div>
    
  
</section>
