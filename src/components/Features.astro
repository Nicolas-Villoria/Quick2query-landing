---
// Features section component
const features = [
  {
    icon: `<svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
    </svg>`,
    title: "Smart Scheduling",
    description: "Automated guard duty assignments that consider preferences, availability, and fairness.",
    bgColor: "bg-blue-100"
  },
  {
    icon: `<svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 0v1.5M9 21h6"></path>
    </svg>`,
    title: "Holiday Planning",
    description: "Coordinate time off requests and ensure adequate coverage during holidays.",
    bgColor: "bg-red-100"
  },
  {
    icon: `<svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
    </svg>`,
    title: "Compliance Ready",
    description: "Built-in compliance tracking and reporting for medical department requirements.",
    bgColor: "bg-green-100"
  }
];
---

<section id="features" class="relative section-padding bg-white overflow-hidden">
  <!-- Modern grid pattern background -->
  <div class="absolute inset-0 -z-10 opacity-40">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 1px 1px, rgba(99, 102, 241, 0.1) 1px, transparent 0); background-size: 40px 40px;"></div>
  </div>
  
  <!-- Floating geometric shapes -->
  <div class="absolute top-20 left-10 w-20 h-20 bg-gradient-to-br from-blue-200 to-blue-300 rounded-3xl rotate-12 opacity-60"></div>
  <div class="absolute top-40 right-20 w-16 h-16 bg-gradient-to-br from-purple-200 to-purple-300 rounded-2xl -rotate-12 opacity-60"></div>
  <div class="absolute bottom-32 left-1/4 w-12 h-12 bg-gradient-to-br from-pink-200 to-pink-300 rounded-xl rotate-45 opacity-60"></div>
  <div class="absolute bottom-20 right-1/3 w-24 h-24 bg-gradient-to-br from-indigo-200 to-indigo-300 rounded-full opacity-40"></div>
  
  <div class="container-custom relative">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-6">Built for Medical Excellence</h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Streamline your department's operations with tools designed specifically for healthcare professionals.
      </p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-12">
      {features.map((feature) => (
        <div class="text-center p-8 bg-white/60 backdrop-blur-sm rounded-3xl ring-1 ring-gray-900/5 shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300 group">
          <div class={`w-20 h-20 ${feature.bgColor} rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 shadow-md`}>
            <Fragment set:html={feature.icon} />
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-4">{feature.title}</h3>
          <p class="text-gray-600 leading-relaxed text-lg">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
  </div>
</section>
