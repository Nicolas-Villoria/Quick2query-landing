---
// Features section component
const features = [
  {
    icon: `<svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
    </svg>`,
    title: "Smart Scheduling",
    description: "Automated guard duty assignments that consider preferences, availability, and fairness.",
    bgColor: "bg-blue-100"
  },
  {
    icon: `<svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 0v1.5M9 21h6"></path>
    </svg>`,
    title: "Holiday Planning",
    description: "Coordinate time off requests and ensure adequate coverage during holidays.",
    bgColor: "bg-red-100"
  },
  {
    icon: `<svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
    </svg>`,
    title: "Compliance Ready",
    description: "Built-in compliance tracking and reporting for medical department requirements.",
    bgColor: "bg-green-100"
  }
];
---

<section id="features" class="section-padding bg-white">
  <div class="container-custom">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-6">Built for Medical Excellence</h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Streamline your department's operations with tools designed specifically for healthcare professionals.
      </p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-12">
      {features.map((feature) => (
        <div class="text-center p-8 group hover:bg-gray-50 rounded-2xl transition-all duration-300">
          <div class={`w-20 h-20 ${feature.bgColor} rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300`}>
            <Fragment set:html={feature.icon} />
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-4">{feature.title}</h3>
          <p class="text-gray-600 leading-relaxed text-lg">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
  </div>
</section>
